# AI 生图 · 多模型并行 · QA 测试建议

> PM 给 QA 的测试**建议**，不是穷举清单。本文档圈出 17 项改动里 PM 视角的「**必测重点 + 易漏边界**」。建议 QA 在自己的测试用例基础上，重点参考本文档第 4、5 章。
>
> 配套：`index.html`（设计稿）+ `ai_image_multimodels_spec.md`（开发 spec）

---

## 0. 文档说明

- **受众**：QA
- **范围**：17 项 P0 改动点 × 美工版 / 运营版 × 三模型
- **不在范围**：phase 1 已稳定运行的功能（除非本期改动涉及）

---

## 1. 通用前置条件

测试前需准备：

1. admin 后台已配置三模型（LinkFox / 自研 / 阿里绘蛙）× 各图片类型上限
2. 测试账号在**美工版**和**运营版**各有数据
3. 至少 1 条 **phase 1 单 LinkFox 老任务**，用于兼容性对比
4. 至少 1 个**未在 admin 配置过的模型**场景（用于"上限 = 0"测试）

---

## 2. 主流程测试

### 2.1 D1 新增任务抽屉

- 打开抽屉：默认仅 LinkFox 启用，数值 = admin 上限
- 启用其他模型（自研 / 绘蛙）：自动按 admin 上限填默认值
- 修改 cell 数值：保存后回显正确
- LinkFox 专属字段（生成图片语言 / 卖点图排版版式 / A+图排版版式）：仅 LinkFox 启用时显示
- 点「保存并提交」：走 M3 校验

### 2.2 D2 编辑任务抽屉

- 已有任务的矩阵全部 cell + LinkFox 专属字段**正确回填**
- 修改任意 cell + 保存：数据持久化
- 取消编辑：原任务数据不变
- **打开 phase 1 单 LinkFox 老任务**：保持单 LinkFox，不自动升级

### 2.3 D3 查看任务抽屉

- 只读（无任何输入框 / 写操作按钮）
- 矩阵**只渲染该任务实际启用的模型列**（未启用的不显示）
- 左下角 `<` `>` 多任务切换浏览正常

### 2.4 M1 批量导入弹窗

- 双版本切换：美工版（SKU + 4 类图片）/ 运营版（ASIN + 5 类图片）
- 矩阵默认值正确（仅 LinkFox 启用 + admin 上限）
- 「确认导入」基础校验：启用模型 ≥1 / 配置数量 ≥1 / 不超 admin 上限
- ⚠️ **不做每日额度校验**：在生图实际提交时（M3）才校验

### 2.5 M2 批量修改弹窗 ⭐

cell 三态语义视觉强区分：

| 输入  | 视觉             | 期望行为                           |
| ----- | ---------------- | ---------------------------------- |
| 留空  | 灰底斜体「不改」 | 提交后该 cell **保持每 SKU 原值**  |
| 0     | 红边红底         | 提交后该 cell 改为 0（清空）       |
| ≥1    | 蓝边蓝底         | 提交后该 cell 改为该值             |

三个快捷按钮：

- **按默认填充**：所有 cell 填 admin 上限
- **全部清零**：所有 cell 填 0
- **全部留空**：所有 cell 设为「不改」

**重点验证**：勾选 N 个 SKU 批量修改，留空的 cell 提交后应保持各 SKU 各自的原值（不被统一改成空）。

### 2.6 M3 提交确认弹窗

- 单条提交：额度够 → 通过；不够 → 阻断
- 批量提交：N 个 SKU 累加占用对某模型不足 → 整体阻断
- 报错文案：使用模型**全名**（"LinkFox 额度不足"，不是 "LF 额度不足"）

---

## 3. 列表层测试

### 3.1 Tab 1-5 筛选 `[模型 ▾]`

- 不选任何模型 → 不过滤，显示全部任务
- 选 1 个模型 → 命中所有启用了该模型的任务
- 选多个模型 → "包含"语义（OR），任务启用任一即命中
- **筛 LinkFox** → 命中所有 phase 1 老任务 + 启用 LinkFox 的新任务

### 3.2 Tab 1/2/3 配置信息列

- pill 行 → 总计划 → 类型 breakdown 显示正确
- **类型层不隐藏 0**：admin 配过的类型即便聚合 0 也显示
- **模型层隐藏全 0**：用户启用但全填 0 的模型不在列表 pill 行显示
- **D1 / D2 编辑态不应用以上规则**（避免编辑过程中 pill 突然消失）

### 3.3 Tab 2 进度列

- 进度条 / 4 个数字（计划 / 成功 / 生成中 / 失败）按"启用模型集合"聚合算
- per-model 明细行：每模型 1 行（1 行 2 模型 grid）
- icon 颜色：✓成功（绿）/ ⏳生成中（紫）/ ✗失败（红）/ 0 值（浅灰）

### 3.4 Tab 3 结果列

- 主体「成功 / 失败」chip + 总耗时
- per-model 明细行：✓成功 + ✗失败

### 3.5 Tab 4/5 全链路列 ⭐

3 行键值结构（计划 → 生成 → 审核）+ per-model 明细行。

**重点验证边界**：

| 场景                                  | 该模型审核位显示                            |
| ------------------------------------- | ------------------------------------------- |
| 该模型生成全失败                      | `—`（**没东西可审**）                       |
| 该模型生成有 N 张成功，审核全没通过   | `0`（审过但全没通过）                       |
| 该模型生成有 N 张成功，审核通过 M 张  | `M`                                         |

`—` 和 `0` 的语义严格区分。

---

## 4. 关键边界态测试 ⭐⭐ 易漏重点

### 4.1 D2 红警特殊态

**前置**：admin 后台已经把某模型某类型的上限改小过（小于原任务存值）。

- 打开该任务的 D2 编辑抽屉
- cell 显示**原存值**（不被截断），且标红（红边红底）
- 不点保存只浏览：值不被自动调整
- 点「保存」或「保存并提交生图」→ 阻断，提示「数量超过上限」
- 改成符合上限的值后保存 → 通过

### 4.2 模型未在 admin 配置过

- D1 / D2 / D3 抽屉中：该模型所有 cell 显示 `≤0`、不可输入
- 矩阵中可见但不可编辑

### 4.3 启用但全 0（"软关闭"）

- 用户启用某模型，但所有 cell 都填 0
- D1 / D2 编辑态：pill 行**保留**该模型 chip
- 列表 Tab123 配置信息列：pill 行**隐藏**该模型
- Tab 2/3/4/5 per-model 行：该模型不显示

### 4.4 复制 / 重跑数据完整性 ⭐

- 单条复制 / 批量复制 / 重跑：**整条原样复制**所有数据
- 验证矩阵全部 cell + LinkFox 专属字段（生成图片语言 / 卖点图排版版式 / A+图排版版式 / 该图对应卖点）**都被复制**
- **复制 phase 1 单 LinkFox 老任务** → 保持单 LinkFox（不自动升级）
- **重跑** = 新增一条新任务（旧任务数据不动）

### 4.5 标普 A+ 图（仅 LinkFox 当前支持）

- D1 / D2 / M1 / M2 矩阵中：**自研 / 绘蛙**的标普 A+ 图 cell 显示 `≤0`、不可输入
- 后续若 admin 调整自研 / 绘蛙的"标普 A+"上限放开 → 前端无需发版即可放开输入
- 测试时可临时改 admin 验证放开效果

### 4.6 admin 配额修改的传递

- admin 改小某模型某类型上限：
  - 新建任务 D1：默认值跟随更新
  - 已存在任务 D2：触发 4.1 红警
  - 列表层：不影响（列表显示的是任务存值）

---

## 5. 兼容性测试 ⭐⭐

### 5.1 phase 1 单 LinkFox 老任务在新 UI 各处的显示

| 位置                  | 期望显示                                  |
| --------------------- | ----------------------------------------- |
| L1 列表 Tab 1/2/3 配置列 | pill 行只显示 LinkFox 一个 chip           |
| L1 列表 Tab 2 进度列  | per-model 明细行只有 LinkFox 一行         |
| L1 列表 Tab 3 结果列  | per-model 明细行只有 LinkFox 一行         |
| L1 列表 Tab 4/5 链路列| per-model 明细行只有 LinkFox 一行         |
| L2 审核工作台         | 分组头微缩 / 卡片角标只显示 LinkFox 单色  |
| L3 沉浸式大屏         | 顶部 Tag 单 LinkFox / 右栏字段正常        |

### 5.2 phase 1 老任务的常规操作

- D2 编辑：可正常打开、修改、保存
- D3 查看：可正常打开
- 复制：保持单 LinkFox
- 重跑：基于老任务创建新任务，仍单 LinkFox
- 筛选 `[模型 ▾]` 选 LinkFox：命中所有老任务

### 5.3 接口分批上线中间态（视技术上线节奏）

- 自研接好但绘蛙没接好：D1 启用绘蛙时是否被禁用 / 提示？
- 三模型分别灰度时，列表层的统计聚合是否正确

---

## 6. 美工版 vs 运营版差异

| 维度       | 美工版         | 运营版                 |
| ---------- | -------------- | ---------------------- |
| 数据维度   | 系统 SKU       | 店铺 ASIN              |
| 图片类型数 | 4 类           | 5 类（含标普 A+）       |
| 标普 A+ 图 | 不涉及         | 涉及，仅 LinkFox 当前支持 |
| 其他功能   | 行为一致       | 行为一致               |

测试时**双版本各跑一遍主流程 + 重点边界**，运营版额外覆盖标普 A+ 相关场景（4.5）。

---

## 7. 文案 / 视觉规范扫描 ⚠️

- 报错 / 提示：使用模型**全名**（"LinkFox" / "自研" / "阿里绘蛙"），不出现 "LF" 这种缩写让运营猜
- 配额返还场景：UI 文案、提示框、接口返回 message 中都应该用「**返还**」或「**释放**」，**不能出现「免费」**
- 模型 chip 配色：LinkFox 蓝 / 自研 绿 / 阿里绘蛙 橙（参考 HTML 设计稿）
- 表格行 / 微缩条用缩写（LF / 自研 / 绘蛙），有完整文案的地方用全名

---

## 8. 推荐覆盖度

| 维度       | 建议覆盖                                                    |
| ---------- | ----------------------------------------------------------- |
| 主流程     | 第 2 章每个 D / M 至少 1 case                              |
| 列表层     | 第 3 章每个 Tab 至少 1 case                                |
| **边界态** | **第 4 章每条至少 1 case ⭐ 重点**                         |
| **兼容性** | **第 5 章 phase 1 老任务在所有新 UI 至少看一遍 ⭐ 重点**    |
| 双版本     | 第 6 章美工版 + 运营版各跑一遍主流程                       |
| 文案规范   | 第 7 章扫一遍                                              |

---

## 9. 附录：HTML 设计稿章节索引

测试时如需对照视觉效果，参考 `index.html`：

| QA 章节               | HTML 设计稿章节            |
| --------------------- | -------------------------- |
| 2.1 / 2.2 / 2.3 抽屉  | S2 / S3 / S4               |
| 2.4 / 2.5 / 2.6 弹窗  | S6 / S7 / S8               |
| 2.5 LinkFox 专属字段  | S5（抽屉）/ S17（L3 大屏）|
| 3.1 筛选              | S10                        |
| 3.2 / 3.3 / 3.4 / 3.5 列展示 | S11 / S12 / S13 / S14 |
| 5.1 L2 审核工作台     | S15                        |
| 5.1 L3 沉浸式大屏     | S16 / S17                  |
| 4.4 复制 / 重跑       | S9                         |

---

**文档版本**：v1.0
