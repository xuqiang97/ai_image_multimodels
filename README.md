# AI 生图 · 多模型并行 · 开发 Spec

> 配套：`index.html`（设计稿，UI/交互细节落点）+ 各模型接口文档（LinkFox 已接、自研 / 阿里绘蛙 PM 单发对接后端）

---

## 0. 文档信息

- **受众**：前端、后端开发
- **改动规模**：17 项 P0 改动点，覆盖**美工版 + 运营版**
- **本 spec 与 HTML 设计稿的关系**：本 spec 承载**业务规则、数据结构、接口契约、状态机、边界 case、命名约定**；UI / 视觉 / 交互细节请对照 HTML 设计稿 S1–S17 章节
- **阅读建议**：
  - FE：通读 第 1、3、4、5 章
  - BE：重点看 第 1、2、4、5 章 + 通用化建议（2.2）

---

## 1. 业务概念

### 1.1 三模型并行 = 协作"配方"关系

不是赛马，不是备份。三模型在同一任务中**并行产出**，每模型承担"配方"中各自的张数与图片类型。任务的最终交付 = 三模型产出的合集。

### 1.2 模型清单

| 名称       | code      | 缩写  | UI 颜色 |
| ---------- | --------- | ----- | ------- |
| LinkFox    | `linkfox` | LF    | 蓝      |
| 自研       | `self`    | 自研  | 绿      |
| 阿里绘蛙   | `huiwa`   | 绘蛙  | 橙      |

> **可扩展性**：未来可能加入豆包等模型。架构 / UI / 数据结构均需为扩展预留（详见 7.4）。

### 1.3 涉及范围

- **美工版**（系统 SKU 维度）：4 类图片（场景图 / 白底特写 / 特写图 / 卖点图）
- **运营版**（店铺 ASIN 维度）：5 类图片（上述 4 类 + 标准普通 A+ 图）
- **L1 任务列表 / L2 审核工作台 / L3 沉浸式大屏**：均受影响

> "标准普通 A+ 图" 当前仅 LinkFox 支持，自研 / 绘蛙暂不支持，详见 3.4。

---

## 2. 后端 Spec

### 2.1 接口架构

每个模型对应一套**异步任务接口**：① 建任务 ② 查结果。三模型结构相同，仅字段命名不同。

| 模型     | 接入状态                           |
| -------- | ---------------------------------- |
| LinkFox  | phase 1 已接，沿用                 |
| 自研     | 本期接入（接口文档由 PM 单发）     |
| 阿里绘蛙 | 本期接入（接口文档由 PM 单发）     |

### 2.2 通用化建议 ⭐

鉴于三模型接口结构同构、字段含义相同（仅命名不同），强烈建议在后端做统一抽象：

- **业务层 / 任务调度 / 配额逻辑共用一套**，不为每个模型重复实现
- 各模型只做**字段命名映射**（外部接口字段 ↔ 内部统一字段）
- 未来加新模型（如豆包）只需扩展字段映射，业务层零改动

PM 已核对各模型接口字段细节（含字段最大字符长度限制、图片各项要求限制）。如有字段对齐疑问，可单独和 PM 梳理。

### 2.3 任务状态机

#### 任务级（不变）

```
待生成 → 生成中 → 已生成
```

#### 子任务级（新增）

子任务粒度 = **每模型 × 每图片类型**，独立调度：

- 任一子任务失败不影响其他子任务
- 任务整体状态由所有子任务汇总判定（详见 5.3）

### 2.4 配额机制

#### 配额来源

admin 后台对 **每个模型 × 每个图片类型** 配置 `max`（每日额度）。FE 只读不写。

#### 流转

| 时机       | 动作       | 描述                                          |
| ---------- | ---------- | --------------------------------------------- |
| 任务提交   | **预占**   | 该模型所有 cell 之和占用对应模型每日额度      |
| 子任务成功 | 不变       | 已占用计入消耗                                |
| 子任务失败 | **返还**   | 该子任务对应张数返还该模型当日额度            |
| 任务取消   | **返还**   | 全部子任务对应张数返还                        |

#### 文案规范 ⚠️

- 配额返还用「**返还**」或「**释放**」
- **禁止使用「免费」措辞**（在任何提示 / 接口返回 message / 日志中均禁止）

#### 边界

- **某模型未在 admin 配置过额度**：所有图片类型 `max = 0` → 矩阵 cell 显示 `≤0`、不可输入
- **某模型 admin 配额改小后，原任务存值 > 当前 max**：D2 编辑抽屉触发红警特殊态（见 3.2）

### 2.5 核心新增字段

任务在 phase 1 字段基础上新增 **多模型矩阵**：

| 字段                     | 说明                                                    |
| ------------------------ | ------------------------------------------------------- |
| 启用模型列表             | 该任务启用了哪几个模型                                  |
| 多模型矩阵数值           | 每模型 × 每图片类型 = 张数                              |
| LinkFox 专属字段（保留） | 生成图片语言、卖点图排版版式、A+图排版版式（见 3.4）    |

phase 1 老任务兼容：等价于「启用模型列表 = [LinkFox]、矩阵仅 LinkFox 列有值」。

### 2.6 命名约定

| 实体      | 格式                  |
| --------- | --------------------- |
| TaskID    | `T20260101_NNNNNN`    |
| SubTaskID | `S20260101_NNNNNN`    |

### 2.7 复制 / 重跑（对应 HTML S9）

#### 业务定义

- **复制 / 批量复制 / 重跑**：业务上都是「基于原任务数据创建一条新记录」
- **重跑 = 新增一条新任务**（无版本号概念，旧任务数据不动）

#### 核心规则：整条原样复制

新建的任务必须**整条原样复制**原任务数据，包括：

1. 启用模型列表
2. 多模型矩阵全部数值
3. 所有 LinkFox 专属字段（生成图片语言 / 卖点图排版版式 / A+ 图排版版式 / 该图对应卖点）

#### ⚠️ 风险点

phase 1 复制逻辑只 copy LinkFox 单字段。本期若沿用旧逻辑、未扩展到矩阵，会出现「**复制后数据残缺**」的隐性回归。务必扩展。

#### 边界

phase 1 单 LinkFox 老任务复制后**保持单 LinkFox**（不自动升级为多模型态）。

---

## 3. 前端 Spec

### 3.1 多模型矩阵组件 ⭐（核心组件）

矩阵布局：横轴 = 模型（LinkFox / 自研 / 阿里绘蛙），纵轴 = 图片类型（美工版 4 类 / 运营版 5 类）。

#### 交互规则

1. 模型 toggle：勾选 = 启用该模型，取消 = 禁用
2. **启用模型时自动填入默认值**（默认值 = admin 配置的每日上限）
3. 单元格数值范围：0 到 admin 上限
4. 越界处理：编辑态可保留越界值，提交时阻断（详见 3.2 D2 红警态）
5. 初始默认：仅 LinkFox 启用 + 数值 = admin 上限
6. 某模型未在 admin 配置 → 该模型所有 cell 上限 = 0、不可输入

#### 复用范围

D1 / D2 / D3 抽屉、M1 / M2 弹窗 共用同一矩阵组件，按场景显示不同视觉态（详见各章节）。

### 3.2 抽屉变体（D1 / D2 / D3）

| 抽屉      | 触发                    | 数据来源       | 可写 | 矩阵特殊态                       |
| --------- | ----------------------- | -------------- | ---- | -------------------------------- |
| D1 新增   | 列表「新增任务」按钮    | 默认值         | 是   | 无                               |
| D2 编辑   | 列表「编辑」按钮        | 任务原值回填   | 是   | **超 max 红警**                  |
| D3 查看   | 列表「查看」按钮        | 任务原值       | 否   | 仅渲染该任务实际启用模型列       |

#### D1 关键流程

1. 矩阵默认只开 LinkFox + 数值 = admin 上限
2. 用户可启用其他模型，启用时自动按默认值填充
3. 点「保存并提交」走 M3 校验额度
4. 若某模型未在 admin 配置过额度，对应所有图片类型默认上限 = 0

#### D2 红警特殊态

admin 改小过该模型该类型的上限，导致原任务存值已超过当前上限 → cell 标红（红边红底）。

- 不自动截断，保留用户原值供自行调整
- 点「保存」或「保存并提交生图」时校验，提示「数量超过上限」

#### D3 只读

- 矩阵**只渲染该任务实际启用的模型列**（不显示未启用的）
- 底部按钮仅「关闭」（无保存动作）
- 左下角 `<` `>` 在多个任务间切换浏览

### 3.3 弹窗变体（M1 / M2 / M3）

#### M1 批量导入（对应 HTML S6）

- SKU/ASIN 文本输入区 + 「已输入 N 行」计数
- **双版本并排呈现**：美工版（SKU + 4 类图片）/ 运营版（ASIN + 5 类图片）
- 模型供应商部分换成多模型矩阵组件（与 D1/D2 完全一致，可交互）
- 「确认导入」**仅做基础校验**（启用模型 ≥1 / 配置数量 ≥1 / 不超 admin 上限），**不校验每日额度**——额度校验在生图任务实际提交时才发生

#### M2 批量修改 ⭐（对应 HTML S7）

**核心 UX：cell 三态语义**

| 状态     | 视觉             | 含义                              |
| -------- | ---------------- | --------------------------------- |
| 留空     | 灰底斜体「不改」 | 不修改（保持每 SKU 各自原值）     |
| 0        | 红边红底         | 改为 0（清空，该 cell 不生成）    |
| ≥1       | 蓝边蓝底         | 改为该值                          |

提供三个快捷按钮：

- **按默认填充**：所有 cell 填入 admin 配置的每日上限
- **全部清零**：所有 cell 填 0
- **全部留空**：所有 cell 设为「不改」（保持各 SKU 原值）

#### M3 提交确认 · 多模型逐行校验（对应 HTML S8）

- 单条 / 批量提交前的额度校验
- 表格逐行展示：每模型本次消耗 / 剩余额度 / 状态
- **任一不足整体阻断**

文案规范：

- 报错使用模型**全名**（"LinkFox" 不是 "LF"，避免运营猜「LF 是什么」）
- 表格行用 **缩写 mini-pill + 全名** 同行（既快速识别又保留完整称呼）

### 3.4 LinkFox 专属字段（对应 HTML S5、S17）

#### 3 个抽屉字段

| 字段           | 显隐规则               | 说明                                                                                              |
| -------------- | ---------------------- | ------------------------------------------------------------------------------------------------- |
| 生成图片语言   | LinkFox 启用时显示     | 下拉选择                                                                                          |
| 卖点图排版版式 | LinkFox 启用时显示     | 多 id 英文逗号分隔，**id 数量需与 LinkFox 卖点图数量一致**（phase 1 已有校验，本期沿用，勿遗漏）  |
| A+ 图排版版式  | LinkFox 启用时显示     | 多 id 英文逗号分隔，**id 数量需与 LinkFox 标普 A+ 图数量一致**（同上）                            |

> 校验规则细节用户已通过「填写说明」帮助文档知悉，UI 上无需重复展示，但**多模型矩阵实现时务必沿用旧校验**。

#### 1 个图片类型：标准普通 A+ 图

- 当前仅 LinkFox 支持
- **自研 / 绘蛙处理方式**：admin 后台先把这两个模型的「标普 A+ 图」`max` 配为 0 → 矩阵 cell 显示 `≤0`、不可输入
- 未来若两模型确认接口支持，调整 admin max 即可放开（**无需改前端代码**）

#### L3 大屏右栏 LinkFox 字段（S17）

- **该图对应卖点**：仅 LinkFox 卖点图显示
- **第三方 id**：phase 1 字段重命名 + 多模型 id 格式备注（黑底视觉，详见 HTML 设计稿）

### 3.5 列表层

#### 筛选（对应 HTML S10）

5 个 Tab 统一在搜索框紧左侧新增 `[模型 ▾]` multi-select dropdown。

规则：

1. **「包含」语义**：选中的任意一个被任务启用即命中
2. 多选 OR 关系：选 LinkFox + 自研 = 任务启用任一即命中
3. 默认空 = 全部任务，不过滤
4. **phase 1 单 LinkFox 老任务自动归为「含 LinkFox」**——筛 LinkFox 时命中所有老任务 + 启用 LinkFox 的新任务

#### 配置信息列（Tab 1/2/3，对应 HTML S11）

显示结构：pill 行 → 总计划 → 类型 breakdown。

聚合规则：

1. **类型层不隐藏 0**：admin 配过的类型即便聚合值是 0 也显示
2. **模型层隐藏全 0**：用户启用了但全填 0 的模型不在 pill 行显示（"软关闭"）
3. **编辑态不应用以上规则**：D1 / D2 抽屉中所有启用 pill 始终保留，避免编辑过程中 pill 突然消失

#### 进度列（Tab 2，对应 HTML S12）

- 进度条按"启用模型集合"聚合占比
- 4 个数字（计划 / 成功 / 生成中 / 失败）按聚合算
- per-model 明细：1 行 2 模型 grid（兼容 4 模型扩展）
- 每行：模型 chip + ✓成功（绿）+ ⏳生成中（紫）+ ✗失败（红）
- phase 1 老任务在新布局下：pill 行只显示 LinkFox 一个 chip

> 后端进度统计算法务必扩展到 matrix 全数据。

#### 结果列（Tab 3，对应 HTML S13）

- 主体保持「成功 / 失败」chip + 总耗时；数字按聚合算
- per-model 明细：1 行 2 模型 grid
- 每行：模型 chip + ✓成功 + ✗失败（直接看 per-model 成败质量，无需下钻 L2）

#### 全链路列（Tab 4/5，对应 HTML S14）

- 3 行键值结构（计划 → 生成 → 审核）保持
- 数字按聚合算
- per-model 明细：1 行 2 模型 grid
- 每行：模型 chip + 计划 → ✓ 生成（绿）→ 👍 审核（蓝）
- per-model 行**只显示成功数**（失败 / 不通过总数在顶部聚合行）
- **边界：某模型生成全失败** → 该模型行的审核位显示 `—`（语义：没东西可审）；与 `0`（审过但全没通过）严格区分

> 后端链路统计算法务必扩展到 matrix。

### 3.6 工作台

#### L2 审核工作台（对应 HTML S15）

改造分两层：

1. **分组头多模型微缩**（中观分布）
2. **卡片三色角标**（微观溯源）

顶部审核状态条保持不变。新增「总计划 N 张」展示。

#### L3 沉浸式大屏（对应 HTML S16、S17）

- 顶部模型 Tag：彩色 Tag（**全名展示**，因空间够且具备来源溯源功能）
- 切图自动刷新 Tag
- 右栏 LinkFox 专属字段适配（黑底视觉）

---

## 4. 17 项改动点 概要对照表

| #  | 章节                       | FE 改动                              | BE 改动                                  |
| -- | -------------------------- | ------------------------------------ | ---------------------------------------- |
| 1  | 顶部额度栏                 | UI 三模型横向布局                    | 三模型额度查询接口                       |
| 2  | D1 新增抽屉                | 矩阵组件、默认值                     | 任务创建接口扩展 matrix                  |
| 3  | D2 编辑抽屉                | 矩阵回填、超 max 红警特殊态          | 任务详情接口扩展 matrix                  |
| 4  | D3 查看抽屉                | 只读矩阵                             | -                                        |
| 5  | LinkFox 专属字段适配（抽屉）| 显隐规则、文案、tooltip              | 字段下发逻辑保持                         |
| 6  | M1 批量导入                | 双版本 + 矩阵组件                    | 批量任务创建接口扩展                     |
| 7  | M2 批量修改                | 三态 cell + 三快捷按钮               | 批量修改接口扩展（含「不改」语义）       |
| 8  | M3 提交校验                | 校验弹窗逐行展示                     | 多模型校验接口                           |
| 9  | Tab5 复制 / 重跑           | -                                    | **整条原样复制 matrix**（核心）          |
| 10 | Tab1-5 筛选                | `[模型 ▾]` dropdown                  | 筛选接口扩展（含 phase 1 老任务归类）    |
| 11 | Tab123 配置列              | 聚合规则、pill 行                    | 列表接口扩展 matrix                      |
| 12 | Tab2 进度列                | per-model 明细行                     | 进度统计算法扩展到 matrix                |
| 13 | Tab3 结果列                | per-model 明细行                     | 结果统计算法扩展到 matrix                |
| 14 | Tab4/5 全链路列            | per-model 明细行                     | 链路统计算法扩展到 matrix（含 `—` 边界） |
| 15 | L2 审核工作台              | 分组头微缩 + 卡片三色角标            | -                                        |
| 16 | L3 顶部 Tag                | 彩色 Tag                             | -                                        |
| 17 | L3 LinkFox 专属字段        | 黑底字段卡                           | -                                        |

---

## 5. 边界与异常

### 5.1 配额相关

| 场景                       | 处理                                                     |
| -------------------------- | -------------------------------------------------------- |
| 模型未在 admin 配置        | `max = 0`，cell 不可输入                                 |
| 配额不足                   | M3 提交时阻断                                            |
| 任务失败 / 取消            | 配额返还（用词「返还」或「释放」，禁止「免费」）         |
| admin 配额改小后原任务超 max | D2 编辑抽屉红警特殊态，提交时阻断                        |

### 5.2 多模型相关

| 场景                                | 处理                                                            |
| ----------------------------------- | --------------------------------------------------------------- |
| 用户启用某模型但全填 0（"软关闭"） | 列表 pill 行隐藏该模型；D1/D2 编辑态保留                       |
| phase 1 单 LinkFox 老任务在新 UI    | 列表 pill 行只显示 LinkFox 一个 chip / per-model 行只显示一行 |
| 复制 phase 1 老任务                 | 保持单 LinkFox（不自动升级为多模型）                           |
| 筛选老任务                          | 自动归类「含 LinkFox」                                          |

### 5.3 部分失败

| 场景                       | 处理                                                              |
| -------------------------- | ----------------------------------------------------------------- |
| 某模型某图片类型生成失败   | 不影响其他子任务                                                  |
| Tab2 进度列                | 失败数 +1，per-model 行 ✗N 高亮                                   |
| Tab3 结果列                | 失败数 +1，per-model 行 ✗N 高亮                                   |
| Tab4/5 全链路 · 生成全失败 | 该模型审核位显示 `—`（与 `0` = 审过全没通过 严格区分）            |

### 5.4 文案规范

| 场景             | 规范                                                  |
| ---------------- | ----------------------------------------------------- |
| 报错 / 提示      | 使用**模型全名**（"LinkFox" 不是 "LF"）               |
| 配额返还描述     | 用「返还」或「释放」，**禁止「免费」**                |
| 表格 / 微缩条    | 用缩写（LF / 自研 / 绘蛙），节省空间                  |
| 模型 chip 配色   | LinkFox 蓝 / 自研 绿 / 阿里绘蛙 橙（详见 HTML 设计稿）|

---

## 6. 阶段交付

本期范围 = 17 项 P0 改动点，详见第 4 章对照表。凡未列入对照表的内容均不在本期。

---

## 7. 附录

### 7.1 HTML 设计稿章节索引

HTML 文件：`index.html`

本 spec 17 章 ↔ HTML S1–S17 一一对应。HTML 是 UI / 视觉 / 交互细节的最终落点，本 spec 与之配套使用。

### 7.2 各模型接口文档

| 模型     | 文档来源                                |
| -------- | --------------------------------------- |
| LinkFox  | phase 1 已接，参考已有代码 / 文档      |
| 自研     | PM 单独发送给后端开发                   |
| 阿里绘蛙 | PM 单独发送给后端开发                   |

PM 已核对各字段细节，包括：

- 字段最大字符长度限制
- 图片各项要求限制（尺寸 / 大小 / 格式 / 数量）
- 接口参数 / 返回值结构
- 异步状态轮询规则

后端开发实现 Adapter 层时，如有字段对齐疑问，可单独和 PM 梳理。

### 7.3 命名约定速查

| 实体          | 格式                  |
| ------------- | --------------------- |
| TaskID        | `T20260101_NNNNNN`    |
| SubTaskID     | `S20260101_NNNNNN`    |

### 7.4 模型扩展机制（未来加新模型，如豆包）

| 层      | 改动                                                                          |
| ------- | ----------------------------------------------------------------------------- |
| 后端    | 新增 Provider Adapter + 注册                                                  |
| admin   | 新增模型 max 配置                                                             |
| 前端    | 模型清单加一项 + 配色 + 文案；矩阵组件、列表 per-model 行均自动支持扩展      |

> 本期 UI 已为 4 模型扩展预留：per-model 明细行的 `1 行 2 模型 grid` 在 4 模型时自然变成 2 行，无需重写。

---

**文档版本**：v1.0
**关联文件**：`index.html`（设计稿）+ 各模型接口文档（独立发送）
